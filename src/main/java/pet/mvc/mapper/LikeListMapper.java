package pet.mvc.mapper;

import java.util.ArrayList;
import java.util.List;

import pet.mvc.board.Board;
import pet.mvc.board.BoardCmt;
import pet.mvc.board.BoardVo;
import pet.mvc.shopservice.*;

public interface LikeListMapper {
	
	void insertLikeList(LikeListMapper dto);

	/*
	 * void Likedelete(long product_code);
	 * 
	 * void addLikeList(LikeList dto);
	 * 
	 * void Likedelete1(long product_code);
	 */
	
	
}
